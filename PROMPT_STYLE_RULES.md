# プロンプト表記ルール

このリポジトリの中学数学プロンプトでは、問題文・解答・解説の文体を以下の方針で統一する。

## 1. 問題文の文体

- 問題文は、生徒への指示として自然な命令形にそろえる。
- 文末は原則として「しなさい。」「求めなさい。」「解きなさい。」の形にする。
- 「ですか。」「でしょうか。」「答えてください。」などの問いかけ調・依頼調は使わない。
- 問題文の最後は句点「。」で終える。

### 用途別の文末

- 計算問題：`次の計算をしなさい。`
- 式変形：`次の式を簡単にしなさい。`
- 展開・因数分解：`次の式を展開しなさい。` / `次の式を因数分解しなさい。`
- 方程式：`次の方程式を解きなさい。`
- 値・数量・長さ・面積など：`〜を求めなさい。`
- 式で表す問題：`〜を式で表しなさい。`
- 選択問題：`〜を選びなさい。`

### 避ける表現

- `何分で追いつきますか？`
- `何個になりますか。`
- `答えてください。`
- `どれですか。`

### 推奨する表現

- `姉は何分で妹に追いつくか求めなさい。`
- `全部で何個になるか求めなさい。`
- `次の問いに答えなさい。`
- `あてはまるものを選びなさい。`

## 2. 解説の文体

- 解説の説明部分は、です・ます調にそろえる。
- 「〜である。」「〜となる。」「〜する。」などの常体は、説明文では原則使わない。
- 計算式の直後に置く結論も、原則として「〜です。」「〜になります。」で終える。
- ステップ見出しは名詞止めでよい。
- 解説の各ステップ見出しの行末には、句点「。」を付けない。

### 用途別の言い回し

- 理由説明：`〜なので、...です。`
- 手順説明：`まず、...します。` / `次に、...します。`
- 式の説明：`この式を解くと、...になります。`
- 結論：`したがって、答えは $...$ です。`
- 注意点：`ここでは、符号に注意します。`

### 避ける表現

- `したがって、答えは $5$ である。`
- `両辺に $2$ をかける。`
- `右辺に移項する。`
- `よって、$x=3$ となる。`

### 推奨する表現

- `したがって、答えは $5$ です。`
- `両辺に $2$ をかけます。`
- `右辺に移項します。`
- `よって、$x=3$ になります。`

## 3. 解答欄の文体

- 【解答】は、原則として答えのみを簡潔に書く。
- 文ではなく数式・数値だけでよい場合は、ディスプレイ数式で示す。
- 単位が必要な場合は、答えに単位を含める。
- 解答の先頭を `=` から始めない。

### 例

```text
【解答】
\[
x = 3
\]
```

```text
【解答】
\[
12\ \text{cm}
\]
```

## 4. 表記チェック項目

- 連立方程式の通常表示では、左辺を1つの数式として書き、`aligned` で `=` の位置だけをそろえる。
- 通常表示では `+` や `-` を独立した列に分けず、LaTeXの標準の数式間隔を使う。
- 加減法の筆算で項を縦にそろえる場面に限り、項を列分けしてよい。
- 問題文が「しなさい。」「求めなさい。」「解きなさい。」型になっているか。
- 問題文に「ですか？」「ますか？」が残っていないか。
- 解説本文がです・ます調で統一されているか。
- 解説本文に「である。」「となる。」「する。」が混在していないか。
- 結論文が「答えは ... です。」または「... になります。」になっているか。
- 【解答】が簡潔で、先頭が `=` になっていないか。

## 5. 解説で使う標準用語

解説のステップ名や手順説明では、できるだけ次の文言に寄せる。
同じ意味の表現が複数ある場合は、下記の表記を優先する。

- 符号の規則を適用する
- 加法を行う
- 減法を加法になおす
- 乗法を行う
- 除法を乗法になおす
- 分数を約分する
- 通分する
- 分数を通分して計算する
- 分数を約分してから計算する
- 計算する順序を判断する
- 数量を文字で表す
- 求める数量を文字で表す
- 文字式のきまりに従って表す
- 数量の関係を式で表す
- 数量の関係を式にする
- 条件を文字式で表す
- 同類項を見つける
- 同類項をまとめる
- 符号に注意して整理する
- 項を整理する
- 式を簡単にする
- 分配法則を使う
- 括弧を外す
- 方程式を立てる
- 両辺に同じ数を加える
- 両辺に同じ数を引く
- 両辺に同じ数をかける
- 両辺を同じ数で割る
- 移項する
- 分母をはらう
- 係数を整数にする
- `ax=b` の形に整理する
- `x=a` の形にする
- 解を求める
- 解を確認する
- 文字について解く
- 式を変形する
- 比例式を変形する
- 分配法則を使って展開する
- 1つ目の括弧を展開する
- 残りの括弧を展開する
- 項どうしを掛ける
- 展開した式を整理する
- 整理する
- 乗法の公式が使えるか判断する
- 乗法の公式にあてはめる
- `(a+b)^2` を展開する
- `(a-b)^2` を展開する
- `(a+b)(a-b)` を展開する
- 展開後に整理する
- 共通因数を見つける
- 各項の共通因数を見つける
- 共通因数でくくり出す
- 各項を因数分解する
- 因数分解の公式が使えるか判断する
- 共通因数でくくったあと公式が使えるか判断する
- `x^2+2ax+a^2` の形を見つける
- `x^2-2ax+a^2` の形を見つける
- `x^2-a^2` の形を見つける
- 公式を適用する
- 因数分解できたか確認する
- 式の値を求める
- 文字に数値を代入する
- 等式を利用して変形する
- 根号を含む数を読み取る
- 平方根を求める
- 根号を簡単にする
- 根号の中を因数分解する
- 根号の中を平方数で分ける
- 根号どうしを加える
- 根号どうしを引く
- 根号どうしを掛ける
- 根号どうしを割る
- 分母を有理化する
- 根号を含む式を整理する
- 因数分解できるか判断する
- 平方完成できるか判断する
- 解の公式を使うか判断する
- 右辺を0にする
- 左辺を整理する
- 因数分解する
- 積が0になる条件を利用する
- 定数項を移項する
- 平方完成する
- 解を整理する
- `ax^2+bx+c=0` の形に整理する
- 係数を読み取る
- 解の公式に代入する
- 判別式を計算する
- 約分する
- 多項式の一部を文字に置き換える
- 共通部分を見つける
- 置き換えた文字で整理する
- 展開する
- 置き換えた文字を元に戻す
